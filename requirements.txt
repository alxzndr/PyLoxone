# Runtime dependencies for the Loxone integration.
# This file mirrors the `requirements` declared in custom_components/loxone/manifest.json
# so that local development and CI match what a Home Assistant install resolves.
# Development and test dependencies live in requirements-dev.txt.
websockets>=14
pycryptodome
httpx
