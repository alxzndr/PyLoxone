# Development and test environment for the PyLoxone integration.
#
# Install on top of the runtime dependencies:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# `pytest-homeassistant-custom-component` pins Home Assistant (2026.8.1) and brings
# in the full HA test harness: pytest, pytest-asyncio, pytest-cov, respx,
# python-dotenv and the `enable_custom_integrations` fixture.
pytest-homeassistant-custom-component==0.13.355

# Linting / formatting (kept in lock-step with the version enforced in CI).
ruff==0.15.20

# Git hooks (see .pre-commit-config.yaml).
pre-commit

# Local debugging in the dev container.
debugpy

# Coloured logging for the connected devcontainer / scripts.
colorlog
