#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

ruff format .
# Blocking lint subset (mirrors the CI `lint` job). The full `ALL` pass is
# advisory until the WP-5.3 lint ratchet clears the style backlog:
#   ruff check . --statistics
ruff check . --select F,E9,PLE,B,T20,S307,ASYNC,RUF006
